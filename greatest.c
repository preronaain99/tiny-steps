#include<stdio.h>

int large(x, y, z)
{
        int lar, small;
        lar = x;
        small = x;
        if(lar > x){
                lar =
        }
}
void main()
{
        int a, b, c;
        scanf("%d %d %d", &a, &b, &c);
        printf("the largest number among %d, %d and %d is %d", a, b, c, large);
}